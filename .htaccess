<IfModule mod_rewrite.c>
    RewriteEngine On

    # Hide /public in the URL when the site root is this folder.
    RewriteRule ^public/ - [L]
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

<Files ".env">
    Require all denied
</Files>
